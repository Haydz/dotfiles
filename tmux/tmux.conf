# tmux configuration
# Linked to ~/.config/tmux by install.sh

# Mouse support: scroll wheel scrolls the pane's scrollback, and clicking
# selects panes/windows and drags borders to resize.
set -g mouse on
